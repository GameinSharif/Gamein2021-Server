<?xml version='1.0' encoding='utf-8'?>

<!DOCTYPE hibernate-configuration PUBLIC
        "-//Hibernate/Hibernate Configuration DTD 3.0//EN"
        "http://www.hibernate.org/dtd/hibernate-configuration-3.0.dtd">

<hibernate-configuration>

    <session-factory>

        <!-- Database connection settings -->
        <property name="connection.driver_class">org.mysql.jdbc.Driver</property>
        <property name="connection.url">jdbc:mysql://localhost:8081/gamein-test</property>
        <property name="connection.username">root</property>
        <property name="connection.password">Arash12313801354</property>

        <!-- JDBC connection pool (use the built-in) -->
        <property name="connection.pool_size">2</property>

        <!-- SQL dialect -->
        <property name="dialect">org.hibernate.dialect.MySQLDialect</property>

        <!-- Disable the second-level cache  -->
        <property name="cache.provider_class">org.hibernate.cache.internal.DisabledCaching</property>

        <!-- Echo all executed SQL to stdout -->
        <property name="show_sql">false</property>

        <!-- Drop and re-create the database schema on startup -->
        <property name="hbm2ddl.auto">validate</property>

        <!-- Names the annotated entity class -->
        <mapping class="ir.sharif.gamein2021.core.entity.User"/>
        <mapping class="ir.sharif.gamein2021.core.entity.Team"/>
        <mapping class="ir.sharif.gamein2021.core.entity.Offer"/>

    </session-factory>

</hibernate-configuration>